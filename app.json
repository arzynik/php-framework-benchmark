{
	"name": "PHP Framework Benchmark",
	"description": "Benchmark multiple frameworks on Heroku",
	"repository": "https://github.com/arzynik/php-framework-benchmark/tree/heroku",
	"scripts": {
		"postdeploy": "sh setup.sh"
	},
	"env": {
		"GITHUB_OAUTH": {
			"description": "An OAuth token from github. Without this composer can not peroperly setup.",
			"required": true,
			"value": ""
		},
		"BENCHMARK_BASE": {
			"description": "The hostname of your new heroku app. Problably something like 'http://appname.herokuapp.com'",
			"required": true,
			"value": "http://appname.herokuapp.com"
		},
		"BENCHMARK_TIME": {
			"description": "The time to wait for requests to finish",
			"required": true,
			"value": "15"
		}
	}
}
